<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.loginsystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/im_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/im_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activity.MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <!-- 默认首页 -->
        <activity android:name=".activity.UserLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.UserRegisterActivity" />
        <activity android:name=".activity.OptionActivity" />
        <activity android:name=".activity.AboutUsActivity" />
        <activity android:name=".activity.UpdatePasswordActivity" />
        <activity android:name=".activity.SetIntervalActivity" />
        <activity android:name=".activity.MyLibraryActivity" />
        <activity android:name=".activity.PracticeActivity" />
        <activity android:name=".activity.UnskillWordActivity" />
        <activity android:name=".activity.ReviewByCNActivity" />
        <activity android:name=".activity.ReviewByENActivity" />
        <activity android:name=".activity.WordTestActivity"></activity>
    </application>

</manifest>